@model FriendlyRS1.ViewModels.UserDetailsVM
@inject SignInManager<ApplicationUser> signInManager;
@using System.Security.Claims;

@{
    Layout = null;
}

<style>
    .list-item {
        padding: .6em 1em;
        list-style: none;
        font-weight: bold;
        transition: all .2s ease;
        border-radius: .3em;
        cursor: pointer;
    }
        .list-item:hover {
            background-color: #5bc0de;
            color: #fff;
        }
        li:focus {
            background-color: #5bc0de;
            color: #fff;
            outline:none;
            border:none;
        }
        .title {
            padding: .6em 1em;
        }

    .about-menu {
        list-style: none;
    }

    aside {
        border-right: 1px solid rgb(233, 232, 232);
    }
</style>


<div class="d-flex rounded border p-3">
    <aside class="d-flex flex-column px-2">
        <p class="text-info m-0 title">ABOUT ME</p>
        <ul class="p-0 about-menu text-secondary">
            <li tabindex="1" onclick="PersonalInfo()" class="list-item">Personal information</li>
            <li tabindex="2" onclick="LivingPlaces()" class="list-item">Living places</li>
            <li tabindex="3" onclick="ContactInfo()" class="list-item">Contact information</li>
        </ul>
    </aside>
    <div>
        <div class="d-flex">
            <div class="flex-grow-1 px-3 " id="information">

            </div>
            @*<div class=" bg-light flex-grow-1">
                    <h3 class="text-info">Home city/country</h3>
                    <hr />
                    <label asp-for="CountryName"></label>
                    <h6>@Model.CountryName</h6>

                    <label asp-for="CityName">City</label>
                    <h6>@Model.CityName</h6>
                    @if (@TempData["edited"] != null)
                    {
                        <div class="alert alert-primary alert-dismissible fade show" role="alert">
                            <strong>@TempData["edited"]</strong>
                            <button type="button" class="close" data-dismiss="alert" aria-label="Close">
                                <span aria-hidden="true">&times;</span>
                            </button>
                        </div>
                    }
                    @if (Model.Me == true)
                    {
                        <button onclick="EditProfileData(@Model.UserId)" class="btn btn-info">Edit Home Details</button>
                    }
                </div>*@
        </div>
    </div>
</div>

